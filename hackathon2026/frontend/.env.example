# Base URL of the FastAPI backend the chat UI talks to.
VITE_API_BASE_URL=http://127.0.0.1:8000

# Set to "true" to use the built-in mock responses (mockApi.js) instead of the
# real backend. Any other value (or unset) uses the real API.
VITE_USE_MOCK=false
